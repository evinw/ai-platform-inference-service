apiVersion: v1
kind: Service
metadata:
  name: inference-service
  namespace: ai-inference
spec:
  selector:
    app: inference-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
